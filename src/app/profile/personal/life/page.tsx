import React from "react";

const Life = () => {
  return (
    <div>
      /** * Outside work I keep things simple — * good conversations, Sahil
      Adeem's podcast, * and handling the house myself. * * I used to game
      daily, broke that habit. * Now it's Wuthering Waves, weekends only, * one
      hour max. * * Not chasing trends. Just building * things that matter. */
    </div>
  );
};

export default Life;
